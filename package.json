{
  "name": "farms-international-site",
  "version": "1.4.0",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "npx http-server src -p 8080 -c-1 -o",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "esbuild": "^0.25.0",
    "http-server": "^14.1.1"
  }
}
